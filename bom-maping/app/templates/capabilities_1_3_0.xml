<?xml version='1.0' encoding="UTF-8"?>
<WMS_Capabilities 
	xmlns="http://www.opengis.net/wms" 
	xmlns:xlink="http://www.w3.org/1999/xlink" 
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
	version="1.3.0"
	xsi:schemaLocation="http://www.opengis.net/wms http://schemas.opengis.net/wms/1.3.0/capabilities_1_3_0.xsd">
	{%- if cap['service'] %}
		{%- set service = cap['service'] %}
	{%- endif %}
	{%- if cap['capability'] %}
		{%- set capability = cap['capability']%}
	{%- endif %}
	<!-- Service Metadata -->
		<Service>
			{%- if service['name'] %}
				<Name>{{service['name']}}</Name>
			{%- endif %}
			{%- if service['title'] %}
				<Title>{{service['title']}}</Title>
			{%- endif %}
			{%- if service['abstract'] %}
				<Abstract>{{service['abstract']}}</Abstract>
			{%- endif %}
		</Service>
	<!-- End Service Metadata -->

	<!-- Capability Data -->
		<Capability>
			{%- if capability['request'] %}
				<Request>{{capability['request']}}</Request>
			{%- endif %}
			{%- if capability['exception'] %}
				<Exception>{{capability['exception']}}</Exception>
			{%- endif %}
			{%- if capability['layer'] %}
				<Layer>{{capability['layer']}}</Layer>
			{%- endif %}
		</Capability>
	<!-- End Capability Data -->
</WMS_Capabilities>